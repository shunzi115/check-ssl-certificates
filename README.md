Monitor the expiry of an SSL certificate, if it expires in less than 30 days, get an alert
```
/etc/zabbix/zabbix_agentd.conf.d/userparameter_certificates.conf
/etc/zabbix/scripts/check_ssl_certificates.py
```
